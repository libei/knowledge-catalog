catalog/
catalog.yaml
.state/
pulled/
!okf/catalog/

# The agent demo's workspace is authored, not pulled: its model and binding
# profile ARE the demo, and its manifest is two static lines.
!agent/catalog/
!agent/catalog.yaml
# The demo's own dependency, resolved on install. Not pinned here: 2,000 lines
# of transitive lockfile would be most of the demo's diff.
agent/package-lock.json
